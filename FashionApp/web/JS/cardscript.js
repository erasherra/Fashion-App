document.addEventListener("DOMContentLoaded", function () {
const createTable = function (let x, let y){
    x = 3;
    y = 5;
    for(let i = 0; i < x; i++){
        let z = document.querySelector("table").insertRow(i);
        for(let c = 0; c < y; c++){
            let v = z.insertCell(c);
            v.
        }
    }
}






}